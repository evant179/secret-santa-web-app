var assert = require('assert');

describe('ExampleTest', function () {
  describe('#exampleTest()', function () {
    it('running example unit test', function () {
      assert.equal(1, 1);
    });
  });
});